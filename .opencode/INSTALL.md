# Installing CodeGate for OpenCode

```json
{
  "plugin": ["/absolute/path/to/codegate"]
}
```

Or after the repo is public:

```json
{
  "plugin": ["codegate@git+https://github.com/carlleilzj/codegate.git"]
}
```

Restart OpenCode. Ask: "What is CodeGate?"
